[build]
command = "dotnet publish src/CultSimulator/CultSimulator.csproj -c Release -o release"
publish = "release/wwwroot"

[build.environment]
DOTNET_VERSION = "9.0"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
